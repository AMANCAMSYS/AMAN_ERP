# تقرير التدقيق الشامل لوحدة التصنيع والإنتاج (Manufacturing & MRP)
# Comprehensive Manufacturing & Production Audit Report

> **التاريخ**: 2026-04-28
> **النطاق**: BOM, Work Orders, Routing, Production Costing, MRP, Shop Floor
> **المراجع**: مهندس إنتاج وخبير أنظمة تخطيط موارد (MRP & Manufacturing Expert)

---

## 1. قائمة المواد ودقة خصم الخامات (BOM & Component Consumption)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **1.1 هيكل BOM** ||||
| 1.1.1 | `core.py` | 337-426 | ✅ سليم | `POST /boms`: إنشاء BOM مع مكونات (components) ومخرجات (outputs). يدعم مكونات بالنسبة المئوية (`is_percentage`) ✓ |
| 1.1.2 | `core.py` | 459-498 | ✅ سليم | `PUT /boms/{id}`: تحديث كامل للـ BOM مع استبدال المكونات والمخرجات |
| 1.1.3 | `database.py` | 3169-3201 | ✅ سليم | جداول `bill_of_materials`, `bom_components`, `bom_outputs` مصممة بشكل صحيح مع `waste_percentage`, `yield_quantity`, `cost_allocation_percentage` |
| **1.2 خصم الخامات عند بدء الإنتاج** ||||
| 1.2.1 | `core.py` | 1024-1064 | ✅ سليم | `start_production_order`: خصم المواد الخام من مستودع المصدر. الحسابات: `required_qty = comp_qty × order_qty × waste_factor` |
| 1.2.2 | `core.py` | 1036-1042 | ✅ سليم | معالجة صحيحة لنوعين من المكونات: ثابت (`comp_qty × order_qty × waste`) ونسبي (`(comp_qty/100 × order_qty) × waste`) |
| 1.2.3 | `core.py` | 1049-1054 | 🔴 P1 | **خطأ في صيغة SQL**: `ON CONFLICT (warehouse_id, product_id) DO UPDATE SET quantity = inventory.quantity - :qty`. مرجع `inventory.quantity` غير صالح في سياق ON CONFLICT. الصحيح هو `SET quantity = EXCLUDED.quantity - :qty` أو الأفضل استخدام `inventory.quantity` في UPDATE منفصل. **هذا السطر سيفشل في وقت التشغيل مما يمنع خصم الخامات!** |
| 1.2.4 | `core.py` | 1047 | 🟠 P2 | خصم المخزون **بدون قفل `FOR UPDATE`** — إذا تم بدء أمرين إنتاج في نفس اللحظة لنفس المادة الخام، قد يحدث تجاوز في المخزون |
| 1.2.5 | `core.py` | 1000-1014 | ✅ سليم | يتحقق من كفاية المواد الخام قبل بدء الإنتاج ويرفض إذا كانت غير كافية |
| **1.3 إضافة المنتج النهائي عند الإكمال** ||||
| 1.3.1 | `core.py` | 1169-1178 | 🟠 P2 | إضافة المنتج النهائي: نفس خطأ `ON CONFLICT` (سطر 1173-1177). `DO UPDATE SET quantity = EXCLUDED.quantity + :qty` يجب أن يكون `inventory.quantity + :qty` أو صيغة صحيحة |
| 1.3.2 | `core.py` | 1189-1210 | ✅ سليم | المنتجات الثانوية (by-products): تُضاف للمخزون بشكل صحيح مع `transaction_type = 'production_in'` |

---

## 2. حساب تكلفة المنتج المصنع (Production Costing)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **2.1 مكونات التكلفة** ||||
| 2.1.1 | `core.py` | 538-611 | ✅ سليم | `calculate_production_cost`: دالة موحدة تشمل 3 مكونات: تكلفة المواد + تكلفة العمالة + التكاليف العامة |
| 2.1.2 | `core.py` | 544-574 | ✅ سليم | **تكلفة المواد**: `Σ(required_qty × unit_cost)` لكل مكون BOM، مع عامل الهدر (`waste_factor = 1 + waste%/100`) |
| 2.1.3 | `core.py` | 581-591 | ✅ سليم | **تكلفة العمالة**: `Σ(actual_run_time/60 × work_center.cost_per_hour)` — يعتمد على وقت التشغيل الفعلي المسجل |
| 2.1.4 | `core.py` | 593-599 | 🟠 P2 | **التكاليف العامة (Overhead)**: `labor_cost × overhead_rate` (افتراضي 30%). **تبسيط شديد**: الـ overhead في التصنيع الحقيقي يشمل إهلاك الآلات، المرافق، العمالة غير المباشرة، الصيانة، ومواد التشغيل. نسبة ثابتة 30% من العمالة لا تعكس التكلفة الحقيقية |
| 2.1.5 | `core.py` | 601-602 | ✅ سليم | تكلفة الوحدة: `total_cost / order_quantity` — صحيح |
| **2.2 توزيع التكلفة على المنتجات الثانوية** ||||
| 2.2.1 | `core.py` | 1240-1246 | 🟠 P2 | `complete_production_order` يجمع تكاليف العمالة من العمليات الفعلية — لكنه **لا يُوزع التكلفة على المنتجات الثانوية** (by-products). المنتجات الثانوية تُضاف للمخزون **بدون تكلفة** |
| 2.2.2 | `models` | — | 🟡 تحذير | حقل `cost_allocation_percentage` موجود في `bom_outputs` لكنه **لا يُستخدم أبدًا** في أي عملية حسابية |
| **2.3 تحديث التكلفة بعد الإنتاج** ||||
| 2.3.1 | `core.py` | 1308-1329 | ✅ سليم | إعادة حساب المتوسط المرجح (WAC) للمنتج النهائي: `wac = (existing_qty × old_cost + new_qty × new_cost) / (existing_qty + new_qty)` |
| 2.3.2 | `core.py` | 1312-1316 | 🟠 P2 | استعلام المخزون الحالي: `SUM(quantity) FROM inventory WHERE product_id = :pid` — يجمع من جميع المستودعات وليس فقط مستودع الوجهة. إذا كان المنتج موجودًا في مستودعات متعددة، سيُحسب WAC خاطئ |
| 2.3.3 | `core.py` | 1316 | 🟠 P2 | طرح الكمية الجديدة: `existing_qty = SUM(quantity) - order.quantity` — منطق هش. الأفضل استخدام `AVG(cost_price)` أو قراءة الكمية قبل الإضافة |
| **2.4 القيد المحاسبي للإنتاج** ||||
| 2.4.1 | `core.py` | 1066-1098 | ✅ سليم | بدء الإنتاج: Dr WIP, Cr Raw Materials — قيد متوازن مع idempotency key |
| 2.4.2 | `core.py` | 1265-1306 | ✅ سليم | إكمال الإنتاج: Dr WIP (labor absorption) + Dr Finished Goods, Cr Absorbed Manufacturing Costs + Cr WIP (FG transfer) |
| 2.4.3 | `core.py` | 1272-1280 | 🟠 P2 | قيد استيعاب العمالة: يفترض وجود `acc_map_labor_cost` أو `acc_map_mfg_overhead`. إذا كانت فارغة، لا يُنشأ قيد استيعاب العمالة — مما يترك WIP غير متوازن مع الـ FG |

---

## 3. دورة حياة أمر العمل (Work Order Lifecycle)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **3.1 نموذج الحالات** ||||
| 3.1.1 | `core.py` | 860-976 | ✅ سليم | `POST /orders`: إنشاء أمر إنتاج بحالة `draft`. ينسخ العمليات من المسار (route). يحسب تكلفة العمالة المعيارية |
| 3.1.2 | `core.py` | 880-888 | ✅ سليم | كشف تلقائي للمسار الافتراضي للمنتج إذا لم يُحدد `route_id` |
| **3.2 الانتقالات** ||||
| 3.2.1 | `core.py` | 977-1129 | ✅ سليم | `draft/confirmed → in_progress`: `POST /orders/{id}/start`. يتحقق من المخزون، يخصم المواد، ينشئ قيد WIP |
| 3.2.2 | `core.py` | 1131-1360 | ✅ سليم | `in_progress → completed`: `POST /orders/{id}/complete`. يضيف منتج نهائي، منتجات ثانوية، يحسب التكلفة، يُحدث WAC، قيد FG |
| 3.2.3 | `core.py` | 1365-1430 | ✅ سليم | `POST /orders/{id}/cancel`: إلغاء الأوامر في حالة `draft` أو `confirmed` فقط. يمنع إلغاء الأوامر قيد التنفيذ |
| **3.3 ثغرات** ||||
| 3.3.1 | `core.py` | — | 🟠 P2 | **لا يوجد endpoint للاعتماد/التأكيد** (approve/confirm). حالة `confirmed` مذكورة في الشروط (سطر 996) لكن لا توجد دالة تنقل الأمر من `draft` إلى `confirmed` |
| 3.3.2 | `core.py` | 876 | 🟡 تحذير | إنشاء رقم أمر الإنتاج: `f"PO-{datetime.now().strftime('%y%m%d%H%M%S')}"` — البادئة `PO-` تشبه Purchase Order وقد تسبب التباسًا. الأفضل `WO-` أو `MO-` |
| 3.3.3 | `core.py` | 1164 | 🟠 P2 | `produced_quantity = quantity` عند الإكمال — يُفترض أن كل الكمية أنتجت. لا يوجد إنتاج جزئي (partial completion). إذا أنتج 80 من 100، يجب إنشاء إدخال منفصل |
| 3.3.4 | `core.py` | 909-918 | 🟠 P2 | نسخ العمليات عند الإنشاء: يفترض أن كل عملية في المسار تحتاج للتنفيذ. لا يوجد منطق لتخطي العمليات (مثل عمليات الفحص الاختيارية) |
| **3.4 Shop Floor (أرضية المصنع)** ||||
| 3.4.1 | `shopfloor.py` | 147-350 | ✅ سليم | `POST /start`: بدء عملية مع تطبيق تسلسل (operation N لا تبدأ قبل N-1) |
| 3.4.2 | `shopfloor.py` | 350+ | ✅ سليم | `POST /complete`: إكمال عملية مع تسجيل الكمية المنتجة والمخردة (scrapped) |
| 3.4.3 | `shopfloor.py` | 73-91 | ✅ سليم | كشف التأخير: ينبه إذا تجاوزت العملية الوقت المخطط بـ 4 ساعات |
| 3.4.4 | `shopfloor.py` | 38-64 | ✅ سليم | WebSocket للبث المباشر للوحة تحكم المشرفين |
| 3.4.5 | `shopfloor.py` | 147 | 🟠 P2 | بدء العملية لا يتحقق من صحة `work_order_id` مقابل `routing_operation_id` — يمكن بدء أي عملية على أي أمر |

---

## 4. الهدر والتالف (Scrap & Waste)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **4.1 الهدر في BOM** ||||
| 4.1.1 | `core.py` | 556 | ✅ سليم | `waste_percentage` على مكونات BOM يُضاف كعامل لزيادة استهلاك المادة الخام. `waste_factor = 1 + waste%/100` |
| 4.1.2 | `core.py` | 564, 570 | ✅ سليم | يُسجل في تفاصيل التكلفة: `waste_qty = total_qty - base_qty` — كمية الهدر محسوبة |
| **4.2 التالف في الإنتاج** ||||
| 4.2.1 | `models` | — | ✅ سليم | `scrapped_quantity` موجود في 3 جداول: `production_orders`, `production_order_operations`, `shop_floor_logs` |
| 4.2.2 | `shopfloor.py` | — | ✅ سليم | `CompleteOperationRequest` يتضمن `scrap_quantity` لتسجيل التالف عند إكمال العملية |
| **4.3 ثغرات** ||||
| 4.3.1 | `core.py` | — | 🔴 P1 | **العائد (yield) غير مستخدم**: حقل `yield_quantity` في `bill_of_materials` **لا يُستخدم في أي معادلة**. لا يوجد حساب لكمية المنتج النهائي = `order_qty × (yield_qty / 100)`. يفترض النظام أن كل الكمية تُنتج بنجاح |
| 4.3.2 | `core.py` | 1164 | 🟠 P2 | `produced_quantity = quantity` — حتى لو كان هناك تالف، يفترض النظام إنتاج الكمية الكاملة |
| 4.3.3 | `core.py` | — | 🟠 P2 | **لا يوجد قيد محاسبي للتالف**: الهدر يُضاف لتكلفة المواد (مقبول)، لكن التالف (scrapped_quantity) لا يُسجل محاسبيًا. لا يوجد Dr حساب تالف/Cr حساب WIP لعكس قيمة الوحدات التالفة |

---

## 5. تخطيط الاحتياجات (MRP)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **5.1 آلية الحساب** ||||
| 5.1.1 | `core.py` | 1765-1870 | ✅ سليم | `GET /mrp/calculate/{order_id}`: يحسب احتياجات المواد لأمر إنتاج واحد |
| 5.1.2 | `core.py` | 1792-1798 | ✅ سليم | يحسب الكمية المطلوبة مع عامل الهدر والمكونات النسبية — نفس منطق BOM في الإنتاج |
| 5.1.3 | `core.py` | 1801-1812 | ✅ سليم | `available = on_hand + on_order`. `shortage = max(0, required - available)` |
| 5.1.4 | `core.py` | 1815-1820 | ✅ سليم | الإجراءات المقترحة: `purchase_order` (إذا كان هناك عجز), `wait_for_po` (إذا طلبيات قيد التنفيذ), `none` |
| **5.2 ثغرات MRP** ||||
| 5.2.1 | `core.py` | 1804-1809 | 🔴 P1 | **استعلام خاطئ للطلبيات المعلقة**: `ON ORDER` يستعلم من `purchase_invoice_items` و `purchase_invoices` بدل `purchase_order_lines` و `purchase_orders`. **فواتير الشراء ≠ أوامر الشراء**. الطلبيات المعلقة الحقيقية يجب أن تُقرأ من PO lines غير المستلمة بالكامل |
| 5.2.2 | `core.py` | 1765 | 🟠 P2 | **MRP لمستوى واحد فقط (Single-Level)**: يحسب احتياجات المواد الخام المباشرة فقط. لا يتحقق من المكونات الفرعية (sub-assemblies) — أي منتج نصف مصنع ضمن BOM لن يُفحص له احتياجات |
| 5.2.3 | `core.py` | — | 🟠 P2 | **لا يأخذ Safety Stock بعين الاعتبار**: `reorder_level` موجود في جدول المنتجات لكنه لا يُستخدم في MRP |
| 5.2.4 | `core.py` | — | 🟠 P2 | **لا يوجد اقتراح أوامر شراء فعلية**: MRP يقترح `purchase_order` كإجراء لكنه لا يُنشئ أمر شراء تلقائيًا. مجرد اقتراح بدون تنفيذ |
| 5.2.5 | `core.py` | 1830 | 🟡 تحذير | `lead_time_days` من المنتج لكنه لا يُستخدم في حساب تاريخ التسليم — لا يوجد موازنة زمنية (time-phased MRP) |
| 5.2.6 | `core.py` | 1765 | 🟠 P2 | **MRP لأمر واحد فقط**: لا يوجد MRP شامل لكل أوامر الإنتاج المعلقة في وقت واحد (net requirements planning). كل أمر يُحسب منعزلًا |

---

## 6. التكامل مع الوحدات الأخرى

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **6.1 التكامل مع المخزون** ||||
| 6.1.1 | `core.py` | 1000-1064 | ✅ سليم | يخصم المواد الخام من المخزون ويُسجل حركات `inventory_transactions` |
| 6.1.2 | `core.py` | 1170-1210 | ✅ سليم | يُضيف المنتج النهائي والمنتجات الثانوية للمخزون |
| **6.2 التكامل المحاسبي** ||||
| 6.2.1 | `core.py` | 1066-1098 | ✅ سليم | قيد WIP عند بدء الإنتاج |
| 6.2.2 | `core.py` | 1265-1306 | ✅ سليم | قيد رسملة FG عند إكمال الإنتاج |
| 6.2.3 | `core.py` | 1094 | ✅ سليم | idempotency key `mfg-start-{order_id}` يمنع التكرار |
| **6.3 التكامل مع الموارد البشرية** ||||
| 6.3.1 | `models` | — | ✅ سليم | `worker_id` في `production_order_operations` و `operator_id` في `shop_floor_logs` — ربط العامل بالعملية |
| 6.3.2 | — | — | 🟡 تحذير | لا يوجد ربط بين ساعات العمل المسجلة في Shop Floor وسجلات حضور الموظفين في HR |
| **6.4 التكامل مع المشتريات** ||||
| 6.4.1 | `core.py` | 1804-1809 | 🔴 P1 | MRP يستعلم بشكل خاطئ عن فواتير الشراء بدل أوامر الشراء |
| 6.4.2 | — | — | 🟠 P2 | لا يوجد تحويل تلقائي من اقتراح MRP إلى أمر شراء فعلي |

---

## 7. مشكلات إضافية

| # | التفصيل | الخطورة |
|---|---------|---------|
| 7.1 | **`ON CONFLICT DO UPDATE` بمرجع خاطئ للجدول**: `inventory.quantity` غير صالح في سياق ON CONFLICT. يجب استخدام اقتباس صحيح أو UPDATE منفصل (`core.py:1053,1176,1199`) | P0 |
| 7.2 | **alembic column naming conflict**: ملف `0012_phase5_world_comparison.py` يضيف أعمدة `yield_quantity`, `waste_percentage` إلخ لكنها معرفة مسبقًا في `database.py` — قد يسبب فشل migration | P1 |
| 7.3 | لا يوجد API لتسجيل إنتاج جزئي (partial completion) — `produced_quantity = quantity` دائمًا | P2 |
| 7.4 | لا يوجد تخطيط سعة (capacity planning) آلي — جدول `capacity_plans` موجود لكن الكود لا يتحقق من الطاقة المتاحة قبل إنشاء أمر الإنتاج | P2 |
| 7.5 | لا يوجد فحص جودة إلزامي (mandatory QC) قبل إكمال الإنتاج — `mfg_qc_checks` موجود لكنه غير مدمج في تدفق complete | P2 |
| 7.6 | `check_inventory_sufficiency` (سطر 634-635) يحتوي على تكرار في حساب `required_qty`: السطر 634 يحسب `base_qty`، والسطر 635 يعيد ضربها في `order.quantity` مرة أخرى للمكونات غير النسبية (خطأ في المعادلة للمكونات الثابتة) | P2 |
| 7.7 | `total_material_cost` في `start_production_order` (سطر 1034) يُعرّف كمتغير محلي لكن `log_activity` (سطر 1106) يحاول استخدامه خارج نطاق `if order.bom_id` — سيسبب `NameError` إذا لم يوجد BOM | P2 |
| 7.8 | إنشاء أمر إنتاج بدون التحقق من وجود صلاحية المستودع المصدر/الوجهة للفرع | P2 |

---

## ملخص الخلل حسب الخطورة

### 🔴 P0 — حرج (يمنع النظام من العمل)
1. **`ON CONFLICT DO UPDATE` بمرجع خاطئ**: `inventory.quantity` غير صالح في SQL — سيفشل خصم وإضافة المخزون في 3 أماكن (`core.py:1053,1176,1199`)

### 🔴 P1 — عالي (خطأ في المنطق أو البيانات)
2. **استعلام MRP خاطئ**: `on_order` يقرأ من فواتير الشراء بدل أوامر الشراء (`core.py:1804-1809`)
3. **العائد (yield) غير مستخدم**: `yield_quantity` في BOM لا يُطبق في أي معادلة إنتاج (`core.py`)
4. **alembic تعارض أعمدة**: migration يضيف أعمدة موجودة مسبقًا

### 🟠 P2 — متوسط
5. خصم المخزون بدون `FOR UPDATE` — سباق بيانات (`core.py:1047`)
6. معادلة `check_inventory_sufficiency` خاطئة للمكونات الثابتة (`core.py:634-635`)
7. `total_material_cost` خارج النطاق في log_activity (`core.py:1106`)
8. لا يوجد حالة `confirmed` منفصلة — لا API للاعتماد (`core.py`)
9. التكاليف العامة (overhead) مبسطة جدًا — 30% ثابت من العمالة (`core.py:598`)
10. لا يوجد إنتاج جزئي — `quantity = produced_quantity` دائمًا (`core.py:1164`)
11. لا يوجد MRP متعدد المستويات أو شامل (`core.py:1765`)
12. المنتجات الثانوية لا تُكلف (`core.py:1189`)
13. WAC يحسب من كل المستودعات وليس مستودع الوجهة فقط (`core.py:1312`)

---

## توصيات التصحيح (حسب الأولوية)

1. **فوري (P0)**: إصلاح صيغة `ON CONFLICT DO UPDATE` في 3 أماكن (`core.py:1053,1176,1199`) إلى `SET quantity = inventory.quantity ± :qty` عبر UPDATE منفصل
2. **فوري (P1)**: إصلاح استعلام MRP `on_order` لقراءة `purchase_order_lines` بدل `purchase_invoice_items`
3. **فوري (P1)**: تفعيل `yield_quantity` في معادلة كمية الإنتاج المتوقعة
4. **عاجل (P1)**: إصلاح تعارض أعمدة alembic مع database.py
5. **عاجل (P2)**: إضافة قفل `FOR UPDATE` على المخزون عند خصم المواد الخام
6. **عاجل (P2)**: إصلاح معادلة `check_inventory_sufficiency` للمكونات الثابتة
7. **هام (P2)**: إضافة API للاعتماد (`POST /orders/{id}/confirm`) للانتقال من draft إلى confirmed
8. **هام (P2)**: دعم الإنتاج الجزئي (partial completion)
9. **هام (P2)**: MRP متعدد المستويات مع safety stock و time-phasing
10. **مستحسن**: تحويل اقتراحات MRP إلى أوامر شراء فعلية تلقائيًا
11. **مستحسن**: دمج فحص الجودة في تدفق إكمال الإنتاج
12. **مستحسن**: تخطيط سعة تلقائي قبل إنشاء أمر الإنتاج
