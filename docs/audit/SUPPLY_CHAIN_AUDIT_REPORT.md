# تقرير التدقيق الشامل لوحدة المشتريات والمخازن (Supply Chain & Inventory)
# Comprehensive Supply Chain & Inventory Audit Report

> **التاريخ**: 2026-04-28
> **النطاق**: Purchase Orders, Stock Movements, Warehouses, Costing, Transfers, Shipments, Landed Costs
> **المراجع**: خبير سلاسل توريد ومخازن (SCM Expert) + مدقق نظم

---

## 1. دقة كميات المخزون (Stock Movement Accuracy)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **1.1 استلام وتسليم (Legacy)** ||||
| 1.1.1 | `stock_movements.py` | 29-131 | 🟡 تحذير | `POST /receipt` و `POST /delivery`: يعملان بشكل صحيح (يزيد/ينقص المخزون) مع قفل `FOR UPDATE`. **لكن لا ينشئان قيدًا محاسبيًا** مما يسبب انحرافًا بين المخزون الفعلي والدفاتر |
| 1.1.2 | `stock_movements.py` | 30, 135 | 🟡 تحذير | كلا الواجهتين موسومتان بـ `Deprecated` في الهيدر لكنهما **غير معطلتين** — أي تكامل قديم ما زال يستخدمهما سيُنتج حركات مخزون دون أثر محاسبي |
| **1.2 تسوية الجرد (Stock Adjustment)** ||||
| 1.2.1 | `stock_movements.py` | 261-424 | ✅ سليم | `POST /adjustment`: يفرض قيدًا محاسبيًا إلزاميًا (Dr/Cr حساب المخزون + حساب التسوية). يمنع المخزون السالب. يحسب `net_value_delta = Σ(qty_delta × unit_cost)` |
| 1.2.2 | `adjustments.py` | 77-274 | ✅ سليم | `POST /adjustments` (قديم): يفرض `new_quantity ≥ 0`. يُحدث المخزون بالقيمة الجديدة مباشرة (وليس الفرق). ينشئ قيدًا محاسبيًا بتكلفة المنتج الحالية |
| 1.2.3 | `adjustments.py` | 193 | 🟠 P2 | قيمة التسوية المحاسبية تستخدم `cost_price` من جدول `products` وليس متوسط التكلفة المرجح الفعلي للمستودع. في سياسة `per_warehouse_wac` قد تكون قيمة القيد غير دقيقة |
| **1.3 حركات الشراء والبيع** ||||
| 1.3.1 | `invoices.py` | 393-407 | ✅ سليم | فاتورة المبيعات: قفل `FOR UPDATE` قبل خصم المخزون. يرفض البيع إذا كانت الكمية غير كافية |
| 1.3.2 | `purchases.py` | 1398-1428 | ✅ سليم | فاتورة المشتريات: يزيد المخزون. يراعي الكميات المستلمة مسبقًا (PO receipt) لتجنب الإضافة المزدوجة |
| 1.3.3 | `pos.py` | 528-538 | ✅ سليم | POS: قفل `FOR UPDATE` قبل الخصم. يرفض البيع إذا كانت الكمية غير كافية |
| **1.4 سجل الحركات** ||||
| 1.4.1 | `stock_movements.py` | 98-113 | ✅ سليم | جميع نقاط الحركة تُسجل في `inventory_transactions` بنوع الحركة والتاريخ والمستخدم |
| 1.4.2 | — | — | 🟡 تحذير | سجل الحركات تراكمي ولا يوجد عملية أرشفة أو تنظيف للبيانات القديمة |

---

## 2. خوارزميات التكلفة (Costing Methods: FIFO, WAC)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **2.1 المتوسط المرجح (WAC)** ||||
| 2.1.1 | `costing_service.py` | 19-41 | ✅ سليم | `calculate_new_cost`: الصيغة الرياضية صحيحة: `(curr_qty × curr_cost + new_qty × new_price) ÷ (curr_qty + new_qty)`. تستخدم Decimal بدقة 4 خانات |
| 2.1.2 | `costing_service.py` | 31-34 | ✅ سليم | حماية ضد حالة فساد البيانات: إذا `current_qty < 0` (مستحيل لكن كحماية)، تُصفر الكمية والتكلفة |
| 2.1.3 | `costing_service.py` | 67-86 | ✅ سليم | `global_wac`: يُجمع كميات جميع المستودعات (`SUM(quantity) FROM inventory`)، يحسب WAC عالمي، يُحدث `products.cost_price` + جميع صفوف `inventory.average_cost` |
| 2.1.4 | `costing_service.py` | 89-130 | ✅ سليم | `per_warehouse_wac`: يحسب WAC لكل مستودع على حدة. يُعيد حساب المتوسط العالمي للمنتج كمتوسط مرجح لجميع المستودعات |
| 2.1.5 | `costing_service.py` | 45, 82 | ✅ سليم | `update_cost` تُستدعى **قبل** تحديث الكمية (لحساب التكلفة بناءً على الحالة الحالية). تعليق واضح في الكود يؤكد هذا الترتيب |
| 2.1.6 | `costing_service.py` | 118-121 | 🟠 P2 | `per_warehouse_wac`: إعادة حساب المتوسط العالمي تستخدم `SUM(quantity * average_cost) / SUM(quantity)` حيث `quantity > 0`. **إذا كان رصيد مستودع سالبًا (نظريًا)، يُستبعد من الحساب** |
| **2.2 FIFO / LIFO** ||||
| 2.2.1 | `costing_service.py` | 213-238 | ✅ سليم | `create_cost_layer`: يُنشئ طبقة تكلفة جديدة عند استلام البضاعة بـ `original_quantity = remaining_quantity = qty` |
| 2.2.2 | `costing_service.py` | 241-317 | ✅ سليم | `consume_layers`: يستهلك الطبقات بترتيب صحيح (FIFO: `ASC`, LIFO: `DESC`). يتحقق من الكمية المتاحة قبل الاستهلاك. يستخدم `FOR UPDATE` لقفل المخزون والطبقات |
| 2.2.3 | `costing_service.py` | 258-273 | ✅ سليم | دستور VIII: يتحقق من `available_quantity = on_hand - reserved_quantity` قبل استهلاك الطبقات |
| 2.2.4 | `costing_service.py` | 320-335 | 🟠 P2 | `handle_return`: **يُعيد المرتجع كطبقة جديدة** بتكلفة الوحدة الأصلية. هذا صحيح للمرتجع لكنه لا يُلغي (reverse) الطبقة الأصلية المستهلكة — الطبقة الأصلية تبقى مستهلكة والمرتجع يُنشئ طبقة جديدة |
| 2.2.5 | `costing_service.py` | 364-427 | ✅ سليم | `change_costing_method`: تجميع الطبقات المتبقية ودمجها في طبقة افتتاحية واحدة بالتكلفة المتوسطة. يستنفد الطبقات القديمة |
| **2.3 التكلفة في التحويلات** ||||
| 2.3.1 | `transfers.py` | 100-103, 311-314 | ✅ سليم | التحويلات تعيد حساب WAC للوجهة: `new_avg_cost = (dest_qty × dest_cost + transfer_qty × source_cost) / (dest_qty + transfer_qty)` |
| 2.3.2 | `shipments.py` | 344-354 | 🟠 P2 | تأكيد الشحنة: يستدعي `CostingService.update_cost()` لتحديث WAC، لكنه **لا يُنشئ طبقة تكلفة** (cost layer) للمنتجات المُدارة بـ FIFO/LIFO. منتجات FIFO تصل للوجهة بدون طبقة تكلفة |
| 2.3.3 | `shipments.py` | 362-378 | 🟡 تحذير | إضافة كمية الوجهة مكررة: يتحقق من وجود صف المخزون مرتين (سطر 358, 367) — أحدهما زائد ولا يضيف قيمة |
| **2.4 التكاليف المُضافة (Landed Costs)** ||||
| 2.4.1 | `landed_costs.py` | 210-360 | ✅ سليم | `allocate`: 4 طرق توزيع (`by_value`, `by_quantity`, `by_weight`, `equal`). يستخدم خوارزمية Largest-Remainder لضبط التقريب (سطر 304-313) |
| 2.4.2 | `landed_costs.py` | 363-400+ | ✅ سليم | `post`: يُحدث `products.cost_price` و `inventory.average_cost` بعد التوزيع. ينشئ قيدًا محاسبيًا. يتحقق من القفل المالي |

---

## 3. مرتجعات المشتريات (Purchase Returns)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **3.1 التحقق من المخزون** ||||
| 3.1.1 | `purchases.py` | 1867-1882 | ✅ سليم | يتحقق من توفر الكمية المراد إرجاعها في المخزون مع قفل `FOR UPDATE`. يرفض إذا تجاوزت الكمية المتاحة |
| 3.1.2 | `purchases.py` | 1881 | 🟡 تحذير | رسالة الخطأ بالعربية فقط وقد لا تظهر بشكل صحيح في السجلات الإنجليزية |
| **3.2 تحديث المخزون** ||||
| 3.2.1 | `purchases.py` | 1931-1936 | ✅ سليم | يخصم الكمية من المخزون: `quantity = quantity - qty`. يسجل في `inventory_transactions` بـ `transaction_type = 'purchase_return'` |
| 3.2.2 | `purchases.py` | 1947 | ✅ سليم | الكمية في سجل الحركات سالبة (`-qty`) مما يعكس حركة خروج |
| **3.3 تحديث رصيد المورد** ||||
| 3.3.1 | `purchases.py` | 1965-1969 | ✅ سليم | يزيد رصيد المورد: `current_balance = current_balance + amount`. المرتجع يعكس الدين (يُقلل الالتزام) |
| **3.4 القيد المحاسبي** ||||
| 3.4.1 | `purchases.py` | 1976-2008 | ✅ سليم | قيد عكسي صحيح: Dr موردون (AP), Cr مخزون + Cr ضريبة مدخلات. يعالج فروق التقريب حتى 0.05 |
| 3.4.2 | `purchases.py` | 1983-1986 | ✅ سليم | `rounding_diff`: يصحح فرق التقريب بين الإجمالي ومجموع الأجزاء (subtotal + tax) بحد أقصى 0.05 |
| 3.4.3 | `purchases.py` | 2010-2072 | ✅ سليم | معالجة الاسترداد النقدي: ينشئ سند قبض للمورد، يُحدث رصيده وقيد المحاسبة |
| **3.5 التكلفة** ||||
| 3.5.1 | `purchases.py` | — | 🟠 P2 | مرتجع الشراء **لا يستخدم `CostingService.handle_return`** لإدارة طبقات FIFO/LIFO. يُنقص المخزون فقط دون إنشاء طبقة تكلفة عكسية. هذا يعني أن تكلفة المخزون المتبقي لن تُعاد حسابها بشكل صحيح في FIFO/LIFO |

---

## 4. تنبيهات الحد الأدنى للمخزون (Reorder Points & Low Stock Alerts)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **4.1 نموذج البيانات** ||||
| 4.1.1 | `inventory_core.py` | 70-71 | ✅ سليم | المنتج يحوي `reorder_level` و `reorder_quantity` |
| **4.2 حساب المخزون المنخفض** ||||
| 4.2.1 | `dashboard.py` | 190-205 | 🟠 P2 | استعلام المخزون المنخفض: `SUM(quantity) <= reorder_level` **لكنه لا يطرح الكميات المحجوزة** (`reserved_quantity`). المخزون المتاح فعليًا = الكمية - المحجوز |
| 4.2.2 | `dashboard.py` | 202 | 🟡 تحذير | منتجات بدون حد إعادة طلب (`reorder_level = 0`) تُعتبر منخفضة إذا كان المخزون ≤ 5. هذا الحد ثابت (5) وغير قابل للتكوين |
| 4.2.3 | `kpi_service.py` | 779-846 | 🟠 P2 | مؤشرات KPI للمخزون المنخفض: لا تطرح المحجوز أيضًا |
| **4.3 التنبيهات التلقائية** ||||
| 4.3.1 | — | — | 🔴 P1 | **لا يوجد نظام تنبيه تلقائي للمخزون المنخفض**. الإعداد `notify_low_stock` موجود في `settings.py:134` لكنه **لا يُستخدم في أي مكان في الكود**. لا يوجد scheduler، لا webhook، لا مهمة خلفية لإرسال إشعارات عند انخفاض المخزون |
| 4.3.2 | `settings.py` | 75 | 🟡 تحذير | `inventory_auto_reorder` مُعرّف كإعداد لكن لا يوجد أي كود ينشئ أوامر شراء تلقائية بناءً عليه |
| 4.3.3 | `webhooks.py` | 110 | 🟡 تحذير | `inventory.low_stock` webhook مُعرّف كحدث لكن لا يوجد كود يُطلقه فعليًا عند انخفاض المخزون |
| **4.4 تقارير المخزون** ||||
| 4.4.1 | `reports.py` | 80-116 | ✅ سليم | `low-stock` في التقارير يعرض المنتجات تحت حد إعادة الطلب |
| 4.4.2 | `StockHome.jsx` | 206 | ✅ سليم | الواجهة الأمامية تعرض عدد المنتجات منخفضة المخزون |

---

## 5. تحويل المخزون بين المستودعات (Stock Transfers)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **5.1 تحويل مباشر (مفرد)** ||||
| 5.1.1 | `transfers.py` | 23-237 | ✅ سليم | `POST /transfers`: قفل `FOR UPDATE` على المصدر والوجهة. تحقق من توفر الكمية. خصم من المصدر. إضافة للوجهة مع WAC. تسجيل في `stock_transfer_log` |
| 5.1.2 | `transfers.py` | 67-70, 83-87 | ✅ سليم | قفل المصدر والوجهة معًا بـ `FOR UPDATE` — يمنع سباق البيانات (race condition) تمامًا |
| 5.1.3 | `transfers.py` | 176-200 | ✅ سليم | قيد محاسبي: Dr مخزون (وجهة), Cr مخزون (مصدر). نفس حساب المخزون — مجرد حركة بين مواقع |
| **5.2 تحويل متعدد الأصناف** ||||
| 5.2.1 | `transfers.py` | 240-430 | ✅ سليم | `POST /transfer`: نفس منطق المفرد لكن بشكل مجمع. قيد محاسبي واحد لجميع الأصناف |
| 5.2.2 | `transfers.py` | 269-272 | ✅ سليم | يتحقق من القفل المالي قبل أي حركة مخزون |
| **5.3 الشحنات (Shipments)** ||||
| 5.3.1 | `shipments.py` | 26-139 | ✅ سليم | `POST /shipments`: إنشاء شحنة بحالة `pending`. **لا يخصم المخزون عند الإنشاء** — فقط عند التأكيد |
| 5.3.2 | `shipments.py` | 279-515 | ✅ سليم | `POST /shipments/{id}/confirm`: يخصم من المصدر مع `FOR UPDATE`، يُضيف للوجهة، يُحدث WAC، يُنشئ إشعارات |
| 5.3.3 | `shipments.py` | 432-473 | ✅ سليم | قيد محاسبي عبر حساب العبور (In-Transit): الساق A: Dr عابر, Cr مخزون. الساق B: Dr مخزون, Cr عابر. الرصيد الصافي = صفر في حساب العبور ✓ |
| 5.3.4 | `shipments.py` | 518-574 | ✅ سليم | `POST /shipments/{id}/cancel`: إلغاء الشحنة قبل التأكيد. لا تأثير على المخزون (لأن المصدر لم يُخصم بعد) |
| **5.4 فروع مختلفة** ||||
| 5.4.1 | `transfers.py` | 52-58 | ✅ سليم | تدقيق الوصول للفروع: يتحقق من أن المستخدم مصرح له بالمستودعين (المصدر والوجهة) |
| 5.4.2 | `shipments.py` | 50-54 | ✅ سليم | نفس التدقيق للشحنات |

---

## 6. المخزون السلبي (Negative Stock Prevention)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **6.1 إعدادات المخزون السلبي** ||||
| 6.1.1 | `settings.py` | 74, 77, 130 | 🟡 تحذير | **ثلاثة إعدادات مختلفة** للمخزون السلبي: `inventory_negative_stock`, `stock_negative_allowed`, `allow_negative_stock`. ازدواجية وتضارب محتمل |
| 6.1.2 | — | — | 🔴 P1 | **لا أحد يقرأ هذه الإعدادات!** جميع نقاط حركة المخزون تمنع المخزون السلبي **بقوة** بغض النظر عن قيمة الإعداد: |
| **6.2 نقاط المنع الفعلية** ||||
| 6.2.1 | `stock_movements.py` | 174-184 | ✅ صارم | `POST /delivery`: يرفض إذا `current_qty < requested_qty` |
| 6.2.2 | `stock_movements.py` | 314-324 | ✅ صارم | `POST /adjustment`: يرفض إذا `current_qty + delta < 0` |
| 6.2.3 | `adjustments.py` | 114-115 | ✅ صارم | `POST /adjustments` (قديم): يرفض إذا `new_quantity < 0` |
| 6.2.4 | `transfers.py` | 76-80 | ✅ صارم | التحويل: يرفض إذا `source_qty < transfer_qty` |
| 6.2.5 | `shipments.py` | 333-335 | ✅ صارم | تأكيد الشحنة: يرفض إذا `src_qty < item.quantity` |
| 6.2.6 | `invoices.py` | 393-407 | ✅ صارم | فاتورة المبيعات: يرفض إذا الكمية غير كافية |
| 6.2.7 | `pos.py` | 528-538 | ✅ صارم | POS: يرفض إذا الكمية غير كافية |
| 6.2.8 | `purchases.py` | 1880-1882 | ✅ صارم | مرتجع المشتريات: يرفض إذا الكمية غير متوفرة للإرجاع |
| 6.2.9 | `costing_service.py` | 268-273 | ✅ صارم | `consume_layers`: يرفض إذا `available_quantity < requested` |
| **6.3 سيناريوهات محتملة للمخزون السلبي** ||||
| 6.3.1 | `stock_movements.py` | 29-131 | 🔴 P1 | `POST /receipt` **لا يمنع المخزون السلبي!** إذا تم إدخال كمية سالبة (خطأ)، سيُخصم المخزون دون فحص. لا يوجد تحقق من `quantity > 0` في الاستلام |
| 6.3.2 | `stock_movements.py` | 87-90 | 🟠 P2 | `POST /receipt`: يستخدم `UPDATE ... SET quantity = quantity + :qty` لكن **بدون `FOR UPDATE`**. إذا تم استلام وشحن نفس المنتج في نفس اللحظة، قد يحدث سباق بيانات |
| 6.3.3 | `invoices.py` | 807-816 | 🟠 P2 | إلغاء فاتورة المبيعات: يعيد المخزون بدون التأكد من عدم تجاوز الحد الأقصى (وهو سلوك صحيح للإلغاء) |

---

## 7. مشكلات إضافية

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| **7.1 ازدواجية APIs** ||||
| 7.1.1 | `stock_movements.py` | 29, 134 | 🟠 P2 | `/receipt` و `/delivery` (قديمين) + `/adjustment` (جديد) + `/adjustments` (قديم آخر) — **4 واجهات مختلفة** لتسوية المخزون بمنطق وصلاحيات مختلفة |
| 7.1.2 | `transfers.py` | 23, 240 | 🟡 تحذير | `/transfers` (مفرد) + `/transfer` (متعدد) — واجهتان مختلفتان لنفس المفهوم |
| **7.2 ثغرات أمنية** ||||
| 7.2.1 | `stock_movements.py` | 29 | 🟠 P2 | صلاحية `stock.manage` للـ `/receipt` و `/delivery` لا تمنع الموظف العادي من التلاعب بالمخزون دون قيد محاسبي |
| 7.2.2 | `stock_movements.py` | 259 | ✅ سليم | صلاحية `stock.adjust` لـ `/adjustment` الجديد — صلاحية أكثر تحديدًا |
| 7.2.3 | `adjustments.py` | 77 | ✅ سليم | صلاحية `stock.adjustment` لـ `/adjustments` — متسقة |
| **7.3 الدقة العددية** ||||
| 7.3.1 | `costing_service.py` | 9 | ✅ سليم | يستخدم `Decimal` في جميع حسابات التكلفة بدقة `_D4` (0.0001) |
| 7.3.2 | `transfers.py` | 103 | 🟠 P2 | حساب WAC في التحويل يستخدم `float` مباشرة (قسمة `float / float`) بدل `Decimal`. خطر خطأ الفاصلة العائمة |
| 7.3.3 | `shipments.py` | 345 | 🟠 P2 | `total_transit_value` يستخدم `Decimal` لكن `source_cost` يأتي كـ `float` من `get_cogs_cost` |
| **7.4 صلاحية المستودع** ||||
| 7.4.1 | `transfers.py` | 38-39 | ✅ سليم | يرفض التحويل لنفس المستودع |
| 7.4.2 | `adjustments.py` | 94-99 | ✅ سليم | يتحقق من صلاحية فرع المستودع |
| 7.4.3 | `purchases.py` | 1224-1227 | ✅ سليم | يتحقق من ارتباط المستودع بالفرع |

---

## ملخص الخلل حسب الخطورة

### 🔴 P1 — عالي (خطر على دقة البيانات أو ثغرة وظيفية)
1. **لا يوجد نظام تنبيه تلقائي للمخزون المنخفض** — إعداد `notify_low_stock` معطل فعليًا. لا scheduler ولا webhook لفعل الأمر (`settings.py:134`, لا مستخدم)
2. **المخزون المنخفض لا يطرح المحجوز** — `reserved_quantity` مهملة في حساب المخزون المتاح فعليًا (`dashboard.py:194-200`, `kpi_service.py:779`)
3. **`POST /receipt` لا يمنع الكميات السالبة** — يمكن إدخال قيمة سالبة عن طريق الخطأ (`stock_movements.py:87-90`)
4. **الشحنات لا تُنشئ طبقات تكلفة FIFO/LIFO للوجهة** — منتجات FIFO تصل بدون طبقة تكلفة (`shipments.py:344-354`)

### 🟠 P2 — متوسط
5. مرتجع المشتريات لا يستخدم `handle_return` لـ FIFO/LIFO (`purchases.py`)
6. قيمة تسوية الجرد تستخدم `cost_price` العام بدل WAC المستودع (`adjustments.py:193`)
7. حساب WAC في التحويلات يستخدم float بدل Decimal (`transfers.py:103`)
8. الواجهات القديمة (deprecated) لا تزال تعمل بدون قيد محاسبي (`stock_movements.py:29,134`)
9. 4 واجهات مختلفة لتسوية المخزون (`stock_movements.py` + `adjustments.py`)
10. ثلاث إعدادات مختلفة للمخزون السلبي لا يقرأها أحد (`settings.py:74,77,130`)
11. `POST /receipt` بدون `FOR UPDATE` — سباق بيانات محتمل (`stock_movements.py:87`)
12. `inventory_auto_reorder` إعداد بدون كود منفذ (`settings.py:75`)

---

## توصيات التصحيح (حسب الأولوية)

1. **فوري (وظيفي)**: بناء نظام تنبيه تلقائي للمخزون المنخفض (scheduler + webhook + إشعار) مع قراءة إعداد `notify_low_stock`
2. **فوري (دقة)**: إضافة طرح `reserved_quantity` من استعلامات المخزون المنخفض لحساب المتاح فعليًا
3. **عاجل (دقة)**: إضافة منع الكميات السالبة في `POST /receipt` (`quantity > 0`)
4. **عاجل (تكلفة)**: إضافة إنشاء طبقات تكلفة للوجهة في تأكيد الشحنات للمنتجات FIFO/LIFO
5. **عاجل (تكلفة)**: استخدام `CostingService.handle_return` في مرتجعات المشتريات
6. **هام (دقة)**: تحويل حسابات WAC في التحويلات إلى Decimal
7. **هام (تنظيف)**: تعطيل أو إزالة الواجهات القديمة (`/receipt`, `/delivery`, `/adjustments` القديم)
8. **هام (إعدادات)**: توحيد إعدادات المخزون السلبي في إعداد واحد وقراءته فعليًا في الكود
9. **مستحسن**: إضافة قفل `FOR UPDATE` على `POST /receipt`
10. **مستحسن**: تنفيذ كود `inventory_auto_reorder` لإنشاء أوامر شراء تلقائية
