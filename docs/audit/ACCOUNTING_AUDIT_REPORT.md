# تقرير التدقيق المحاسبي الشامل — AMAN ERP
# Comprehensive Accounting Audit Report

> **التاريخ**: 2026-04-28
> **النطاق**: وحدة المحاسبة (Journal Entries, General Ledger, Chart of Accounts, Taxes, Forex)
> **المراجع**: محاسب قانوني (CPA) + مطور Backend مالي

---

## 1. معادلة القيد المزدوج (Double-Entry: Debit = Credit)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 1.1 | `gl_service.py` | 20-55 | ✅ سليم | دالة `validate_je_lines` تفرض 4 شروط: سطر واحد على الأقل، لا قيم سالبة، لا مدين ودائن معًا، الفرق ≤ 0.01 |
| 1.2 | `utils/accounting.py` | 20-61 | 🔴 P1 | توجد **دالتان تحقّق مختلفتان**: `validate_je_lines` في `gl_service.py` تقبل سطرًا واحدًا (`if not lines` سطر 33)، بينما نظيرتها في `utils/accounting.py` ترفض إذا `len(valid) < 2` (سطر 42). هذا تناقض خطير — قيد ذاتي التوليد قد يُرفض في مكان ويُقبل في آخر |
| 1.3 | `utils/accounting.py` | 50-51 | 🟠 P2 | تستخدم جمع `float` مباشر `sum(l.get("debit", 0))` بدل `Decimal` — يُعرّض لخطأ الفاصلة العائمة في مقارنة التوازن |
| 1.4 | `purchases.py` | 1276-1284 | 🟠 P2 | استدعاء `compute_invoice_totals` بدون تمرير `markup_amount` و `header_discount_pct` — أي هامش ربح أو خصم رأسي في فاتورة مشتريات لن يُحتسب في الإجمالي |
| 1.5 | `invoices.py` | 225-229, 558-571 | 🔴 P1 | **خلل جسيم**: الـ `markup` يُضاف إلى `grand_total` (الطرف المدين) لكنه **لا يُضاف** إلى الأطراف الدائنة (Revenue + VAT). **عند وجود markup، القيد الناتج غير متوازن بقيمة الـ markup**! |
| | | | | مثال: مبيعات 100 + ضريبة 15 + markup 10 = إجمالي 125. المدين: 125 (صندوق/مدينون) + COGS. الدائن: 100 (إيرادات) + 15 (ضريبة) + COGS. **الفرق = 10 (قيمة markup المفقودة)** |
| 1.6 | `gl_service.py` | 52 | 🟡 تحذير | التسامح `> _D2` (0.01) واسع جدًا — فروق 0.009 تمر دون إنذار. الأفضل استخدام `> Decimal('0.005')` |

---

## 2. إشارات الأرقام (Sign Convention)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 2.1 | `utils/accounting.py` | 133-171 | ✅ سليم | `update_account_balance`: الأصول/المصروفات = `debit - credit` (يزيد بالمدين). الخصوم/حقوق الملكية/الإيرادات = `credit - debit` (تزيد بالدائن). التنفيذ صحيح 100% |
| 2.2 | `accounting.py` | 1376-1410 | ✅ سليم | إقفال السنة: مدين ← يعدم الرصيد الدائن للإيرادات. دائن ← يعدم الرصيد المدين للمصاريف. ترحيل الربح لدائن الأرباح المبقاة. صحيح |
| 2.3 | `accounting.py` | 2357-2455 | ✅ سليم | `generate_closing_entries`: نفس المنطق الصحيح مع خيار ملخص الدخل كحساب وسيط |
| 2.4 | `invoices.py` | 531-585 | ✅ سليم | قيد المبيعات: Dr صندوق/مدينون + Dr COGS, Cr إيرادات + Cr ض.مخرجات + Cr مخزون. الإشارات صحيحة |
| 2.5 | `purchases.py` | 1539-1615 | ✅ سليم | قيد المشتريات: Dr مخزون + Dr ض.مدخلات, Cr صندوق/موردون. الإشارات صحيحة |
| 2.6 | `currencies.py` | 362-368 | ✅ سليم | إعادة التقييم: ربح ← Dr الحساب + Cr أرباح غير محققة. خسارة ← Cr الحساب + Dr خسائر غير محققة. صحيح |

---

## 3. إغلاق الفترات المحاسبية (Period Closing)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 3.1 | `fiscal_lock.py` | 14-73 | 🔴 P1 | يستخدم جدول `fiscal_period_locks` (حقل `is_locked`) |
| 3.2 | `gl_service.py` | 122-131 | 🔴 P1 | يستخدم جدول `fiscal_periods` (حقل `is_closed`) |
| 3.3 | — | — | 🔴 P1 | **آليتا قفل مختلفتان لنفس الغرض**: `fiscal_periods.is_closed` ≠ `fiscal_period_locks.is_locked`. وجود نظامي قفل متوازيين يخلق ثغرات عدم اتساق |
| 3.4 | `invoices.py` | 590 | 🔴 P1 | فاتورة المبيعات **لا تستدعي** `check_fiscal_period_open` مطلقًا! تعتمد فقط على `gl_service.py` الداخلي الذي يفحص `fiscal_periods`. **إذا قام المدير بقفل فترة عبر `fiscal_period_locks`، ستمر فواتير المبيعات دون منع** |
| 3.5 | `accounting.py` | 1426-1431 | 🟠 P2 | `close_fiscal_year` يغلق `fiscal_periods.is_closed` لكنه **لا يحدّث** `fiscal_period_locks.is_locked` — تناقض بين الجدولين عند الإقفال |
| 3.6 | `accounting.py` | 606 | 🟡 تحذير | القيود المسودة (draft) تُفحص ضد الفترة المغلقة — منطقيًا القيد كمسودة لا يؤثر على الأرصدة لكن النظام يمنعه |
| 3.7 | `purchases.py` | 916, 1216 | ✅ سليم | فاتورة المشتريات واستلام البضاعة يستدعيان `check_fiscal_period_open` بشكل صحيح |
| 3.8 | `taxes.py` | 690, 1062 | ✅ سليم | دفع الضريبة والتسوية الضريبية يستدعيان `check_fiscal_period_open` بشكل صحيح |

---

## 4. حسابات الضريبة (VAT/ZATCA)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 4.1 | `taxes.py` | 364-510 | ✅ سليم | `create_tax_return`: يحسب VAT المخرجات - مردودات المبيعات - VAT المدخلات + مردودات المشتريات. يشمل فترات ربع وشهرية. صحيح |
| 4.2 | `taxes.py` | 651-764 | ✅ سليم | `create_tax_payment`: ينشئ قيد Dr ضريبة مخرجات, Cr بنك. يتحقق من القفل المالي. يمنع الدفع الزائد |
| 4.3 | `taxes.py` | 1009-1113 | 🔴 P1 | `create_tax_settlement`: يحسب VAT من `sales` و `purchase` فقط، **لا يشمل** `sales_return` و `purchase_return`. `create_tax_return` يحسبها بشكل صحيح، لكن التسوية لا. **التسوية الضريبية غير دقيقة** |
| 4.4 | `taxes.py` | 769-839 | ✅ سليم | `get_vat_report`: يشمل جميع الأنواع (sales, purchase, sales_return, purchase_return) |
| 4.5 | `invoices.py` | 567-571 | ✅ سليم | قيد المبيعات: يسجل VAT Output في حساب `acc_map_vat_out` |
| 4.6 | `purchases.py` | 1568-1572 | ✅ سليم | قيد المشتريات: يسجل VAT Input في حساب `acc_map_vat_in` |
| 4.7 | `industry_coa_templates.py` | 60, 77 | 🟠 P2 | حسابات VAT (15010: ضريبة مدخلات, 21040: ضريبة مخرجات) موجودة في شجرة الحسابات لكن **مفاتيح الربط** (`acc_map_vat_in`, `acc_map_vat_out`) لا تُسجل تلقائيًا في `company_settings` عند زراعة COA. يجب على كل مستأجر تعيينها يدويًا وإلا تفشل جميع القيود الضريبية |
| 4.8 | `utils/accounting.py` | 201-241 | ✅ سليم | `compute_invoice_totals`: خصم الرأس (header discount) يُنقص الضريبة **نسبيًا** (ZATCA-compliant: discount reduces tax proportionally) |

---

## 5. صرف العملات (Forex)

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 5.1 | `currencies.py` | 297-305 | 🔴 P0 | **فحص التكرار معطل تمامًا**: `entry_number LIKE 'REV-{code}-%'` لكن `entry_number` دائمًا بصيغة `JE-XXXXX` (يولده `generate_sequential_number` بالبادئة `JE`). التعبير `REV-SAR-%` لن يتطابق مع `JE-00001` أبدًا. **يمكن إنشاء تقييمات مكررة غير محدودة لنفس العملة في نفس التاريخ** |
| 5.2 | `currencies.py` | 314-318 | 🔴 P0 | **حسابات غير موجودة**: `UFX-GAIN` و `UFX-LOSS` رموز ثابتة غير موجودة في **أي** من قوالب COA الـ 12 ولا في الحسابات الأساسية. الدالة ستفشل دائمًا بـ HTTP 500 لأي مستأجر لم يُنشئ هذين الحسابين يدويًا |
| 5.3 | `currencies.py` | 331-341 | 🔴 P1 | **اتجاه FC balance معكوس للخصوم**: الـ SQL يحسب رصيد العملة الأجنبية وكأن جميع الحسابات أصول: |
| | | | | ```sql CASE WHEN debit > 0 THEN +amount_currency WHEN credit > 0 THEN -amount_currency ``` |
| | | | | للأصول: Dr يزيد ← صحيح. للخصوم: **Cr يزيد الرصيد** لكن الكود يُعطي قيمة سالبة. إعادة تقييم حسابات الخصوم بالعملة الأجنبية **ستحسب أرباح/خسائر بعكس الاتجاه الصحيح** |
| 5.4 | `gl_service.py` | 168 | 🟠 P2 | العملة الأساسية الافتراضية `SYP` (ليرة سورية) عند فقدان الإعدادات — غير متسقة مع الزرع الافتراضي SAR |
| 5.5 | `utils/accounting.py` | 131 | 🟠 P2 | نفس السقوط إلى `SYP` في `get_base_currency` |
| 5.6 | `purchases.py` | 1194-1206 | ✅ سليم | يتحقق صراحةً من وجود سعر الصرف للعملات الأجنبية ويرفض إذا كان مفقودًا |
| 5.7 | `gl_service.py` | 222-226 | ✅ سليم | يحسب `debit_base = input_debit × rate` و `credit_base = input_credit × rate` بدقة Decimal |

---

## 6. التكامل بين الفواتير والقيود التلقائية

| # | الملف | السطر | الخطورة | التفصيل |
|---|-------|-------|---------|---------|
| 6.1 | `invoices.py` | 520-602 | 🟠 P2 | فاتورة مبيعات → قيد تلقائي يشمل Cash/AR + COGS + Revenue + VAT + Inventory. **الخلل الوحيد**: markup غير متوازن (انظر 1.5) |
| 6.2 | `purchases.py` | 1514-1632 | ✅ سليم | فاتورة مشتريات → قيد تلقائي مع عكس مستحقات أمر الشراء (PO accrual reversal). المتغيرات تتوازن: `Dr(Inventory + Unbilled + VAT) = Cr(Cash + AP)` |
| 6.3 | `purchases.py` | 914-941 | ✅ سليم | استلام بضاعة (PO receipt) → قيد مستحق Dr مخزون, Cr مشتريات غير مفوترة |
| 6.4 | `sales_improvements.py` | 345-363 | ✅ سليم | صرف عمولات → قيد Dr مصروف عمولة, Cr بنك/صندوق. مع مفتاح Idempotency لمنع التكرار |
| 6.5 | `taxes.py` | 688-739 | ✅ سليم | دفع ضريبة → قيد Dr ض.مخرجات, Cr بنك |
| 6.6 | `accounting.py` | 949-1091 | ✅ سليم | إلغاء القيد → قيد عكسي (swap debit/credit) عبر `gl_create_journal_entry` مع فحص صلاحيات المصدر |
| 6.7 | `gl_service.py` | 110-120 | ✅ سليم | حماية من التكرار عبر `source + source_id + date` — يمنع ترحيل مزدوج من نفس الحدث |
| 6.8 | `gl_service.py` | 94-102 | ✅ سليم | حماية Idempotency-Key مع معالجة race condition عند `IntegrityError` |
| 6.9 | `invoices.py` | 589-599 | 🟡 تحذير | `source="Sales-Invoice"` (حالة أحرف مختلطة) بينما باقي المصادر lowercase. الـ event map تعمل بسبب `.lower()` لكنه غير متسق |

---

## 7. مشكلات معمارية / هيكلية

| # | الملف | التفصيل |
|---|-------|---------|
| 7.1 | `gl_service.py` + `utils/accounting.py` | تكرار كود التحقق `validate_je_lines` في ملفين بمنطق مختلف |
| 7.2 | `models/` + `database.py` | تعريف الجداول مكرر: SQLAlchemy ORM + raw SQL — خطر انحراف المخطط (schema drift) |
| 7.3 | `fiscal_lock.py` + `gl_service.py` | نظاما قفل متوازيان لنفس الغرض دون تنسيق |
| 7.4 | `industry_coa_templates.py` | مفاتيح ربط الحسابات (account mappings) لا تُزرع تلقائيًا مع COA |
| 7.5 | `accounting.py:598` | استيراد مكرر لـ `gl_create_journal_entry` داخل الدالة مع وجود استيراد على مستوى الموديول (سطر 18) |

---

## ملخص الخلل حسب الخطورة

### 🔴 P0 — حرج (يؤدي لاختلال الميزانية أو فشل النظام)
1. **إعادة التقييم: فحص التكرار معطل** (`currencies.py:297-305`) — قيود تقييم مكررة تضخم الأرباح/الخسائر
2. **إعادة التقييم: حسابات UFX-GAIN/UFX-LOSS غير موجودة** (`currencies.py:314-318`) — فشل 500 دائمًا

### 🔴 P1 — عالي (خطأ في المنطق الحسابي)
3. **قيد مبيعات غير متوازن مع markup** (`invoices.py:558-571`) — فرق = قيمة markup
4. **إعادة تقييم: FC balance معكوس للخصوم** (`currencies.py:331-341`)
5. **آليتا قفل مزدوجتان** (`fiscal_lock.py` + `gl_service.py`) — ثغرة فواتير المبيعات في فترات مقفلة
6. **تسوية ضريبية لا تشمل المرتجعات** (`taxes.py:1030-1044`)

### 🟠 P2 — متوسط
7. دالتا تحقق مختلفتان (`gl_service.py:33` vs `utils/accounting.py:41`)
8. جمع float بدل Decimal (`utils/accounting.py:50-51`)
9. فاتورة مشتريات: markup/header discount مفقودان (`purchases.py:1276`)
10. سقوط العملة الأساسية لـ SYP (`gl_service.py:168`, `utils/accounting.py:131`)
11. مفاتيح ربط VAT لا تُزرع تلقائيًا

---

## توصيات التصحيح (حسب الأولوية)

1. **فوري**: إصلاح حسابات `UFX-GAIN`/`UFX-LOSS` — إما إضافتها لقالب COA الأساسي أو جعل البحث ديناميكيًا عبر `get_mapped_account_id`
2. **فوري**: إصلاح فحص تكرار إعادة التقييم — تغيير `entry_number LIKE` إلى `reference LIKE` أو استخدام `source + source_id`
3. **عاجل**: إضافة سطر markup إلى الجانب الدائن في قيد المبيعات (Cr حساب إيرادات إضافي أو حساب markup منفصل)
4. **عاجل**: توحيد آلية قفل الفترات في آلية واحدة وإضافة استدعاء `check_fiscal_period_open` في `invoices.py`
5. **عاجل**: إصلاح حساب FC balance للخصوم في `currencies.py:331-341` (عكس الإشارة أو استخدام `account_type`)
6. **هام**: إضافة `sales_return` و `purchase_return` إلى `create_tax_settlement`
7. **هام**: توحيد دالتي `validate_je_lines` في دالة واحدة
8. **مستحسن**: زرع مفاتيح ربط الحسابات (`acc_map_vat_in`, `acc_map_vat_out`, إلخ) تلقائيًا عند زراعة COA
