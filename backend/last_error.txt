Error: 500: فشل إنشاء الجداول: (psycopg2.errors.SyntaxError) syntax error at or near "("
LINE 13:         UNIQUE(year, month_number, COALESCE(branch_id, 0), C...
                                                    ^

[SQL: -- ===== SALES TARGETS =====
    CREATE TABLE IF NOT EXISTS sales_targets (
        id SERIAL PRIMARY KEY,
        year INTEGER NOT NULL,
        month_number INTEGER NOT NULL,
        target_amount DECIMAL(18, 4) DEFAULT 0,
        branch_id INTEGER REFERENCES branches(id),
        salesperson_id INTEGER,
        notes TEXT,
        created_by INTEGER REFERENCES company_users(id),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(year, month_number, COALESCE(branch_id, 0), COALESCE(salesperson_id, 0))
    );]
(Background on this error at: https://sqlalche.me/e/20/f405)
Traceback (most recent call last):
  File "/home/omar/Desktop/aman/backend/routers/companies.py", line 115, in register_new_company
    raise HTTPException(status_code=500, detail=f"فشل إنشاء الجداول: {message}")
fastapi.exceptions.HTTPException: 500: فشل إنشاء الجداول: (psycopg2.errors.SyntaxError) syntax error at or near "("
LINE 13:         UNIQUE(year, month_number, COALESCE(branch_id, 0), C...
                                                    ^

[SQL: -- ===== SALES TARGETS =====
    CREATE TABLE IF NOT EXISTS sales_targets (
        id SERIAL PRIMARY KEY,
        year INTEGER NOT NULL,
        month_number INTEGER NOT NULL,
        target_amount DECIMAL(18, 4) DEFAULT 0,
        branch_id INTEGER REFERENCES branches(id),
        salesperson_id INTEGER,
        notes TEXT,
        created_by INTEGER REFERENCES company_users(id),
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(year, month_number, COALESCE(branch_id, 0), COALESCE(salesperson_id, 0))
    );]
(Background on this error at: https://sqlalche.me/e/20/f405)
