"""
TASK-041 — Plugins root.

Drop a sub-package here (e.g. plugins/my_integration/__init__.py) with a
`register(app, bus)` callable. It will be auto-discovered at startup.
See backend/utils/plugin_registry.py for the contract.
"""
