# AMAN ERP — Development / CI requirements.
# Install with: pip install -r backend/requirements.txt -r backend/requirements-dev.txt
# Runtime deps live in backend/requirements.txt.

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-xdist>=3.5
httpx>=0.27
requests-mock>=1.12

# Static analysis / formatting (optional, matches pre-commit hooks)
ruff>=0.4
